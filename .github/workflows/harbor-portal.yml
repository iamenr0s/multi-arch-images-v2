name: Build and Push harbor-portal

on:
  push:
    branches:
      - main
    paths:
      - 'harbor-portal/**'
      - '.github/workflows/harbor-portal.yml'
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build multi-arch image
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Get upstream harbor version info
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          LATEST_TAG=$(curl -sf -H "Authorization: Bearer ${GH_TOKEN}" \
            https://api.github.com/repos/goharbor/harbor/releases/latest | jq -r '.tag_name // empty')
          if [[ -z "${LATEST_TAG}" ]]; then
            echo "ERROR: Could not determine upstream harbor version" >&2
            exit 1
          fi
          TAG_OBJ=$(curl -sf -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/goharbor/harbor/git/ref/tags/${LATEST_TAG}")
          TAG_SHA=$(echo "${TAG_OBJ}" | jq -r '.object.sha')
          TAG_TYPE=$(echo "${TAG_OBJ}" | jq -r '.object.type')
          if [[ "${TAG_TYPE}" == "tag" ]]; then
            LATEST_COMMIT=$(curl -sf -H "Authorization: Bearer ${GH_TOKEN}" \
              "https://api.github.com/repos/goharbor/harbor/git/tags/${TAG_SHA}" \
              | jq -r '.object.sha' | cut -c1-7)
          else
            LATEST_COMMIT=$(echo "${TAG_SHA}" | cut -c1-7)
          fi
          BUILD_DATE=$(date -u +%Y-%m-%d)
          echo "version=${LATEST_TAG}" >> "$GITHUB_OUTPUT"
          echo "commit=${LATEST_COMMIT}" >> "$GITHUB_OUTPUT"
          echo "build_date=${BUILD_DATE}" >> "$GITHUB_OUTPUT"

      - name: Set short SHA tag
        id: tags
        env:
          GITHUB_SHA_FULL: ${{ github.sha }}
        run: |
          SHORT_SHA=$(echo "${GITHUB_SHA_FULL}" | cut -c1-7)
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build each arch with its own local tag (not a shared --manifest). Sharing
      # --manifest across two sequential multi-stage builds with a BUILDPLATFORM
      # stage makes podman reuse the first build's runtime layers and ship an
      # "arm64" image with amd64 content. Distinct per-arch tags avoid that.
      - name: Build amd64
        env:
          VERSION: ${{ steps.upstream.outputs.version }}
          COMMIT: ${{ steps.upstream.outputs.commit }}
          BUILD_DATE: ${{ steps.upstream.outputs.build_date }}
        run: |
          podman build \
            --platform linux/amd64 \
            -t localhost/harbor-portal:amd64 \
            --build-arg "VERSION=${VERSION}" \
            --build-arg "COMMIT=${COMMIT}" \
            --build-arg "BUILD_DATE=${BUILD_DATE}" \
            --build-arg "HARBOR_REF=${VERSION}" \
            -f harbor-portal/Dockerfile \
            harbor-portal/

      - name: Build arm64
        env:
          VERSION: ${{ steps.upstream.outputs.version }}
          COMMIT: ${{ steps.upstream.outputs.commit }}
          BUILD_DATE: ${{ steps.upstream.outputs.build_date }}
        run: |
          podman build \
            --platform linux/arm64 \
            -t localhost/harbor-portal:arm64 \
            --build-arg "VERSION=${VERSION}" \
            --build-arg "COMMIT=${COMMIT}" \
            --build-arg "BUILD_DATE=${BUILD_DATE}" \
            --build-arg "HARBOR_REF=${VERSION}" \
            -f harbor-portal/Dockerfile \
            harbor-portal/

      - name: Assemble manifest
        run: |
          podman manifest create harbor-portal
          podman manifest add harbor-portal containers-storage:localhost/harbor-portal:amd64
          podman manifest add harbor-portal containers-storage:localhost/harbor-portal:arm64
          podman manifest inspect harbor-portal

      - name: Push to Quay.io
        env:
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
        run: |
          podman manifest push --all harbor-portal \
            quay.io/iamenr0s/harbor-portal:latest
          podman manifest push --all harbor-portal \
            "quay.io/iamenr0s/harbor-portal:${SHORT_SHA}"

      - name: Push to Docker Hub
        env:
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
        run: |
          podman manifest push --all harbor-portal \
            docker.io/iamenr0s/harbor-portal:latest
          podman manifest push --all harbor-portal \
            "docker.io/iamenr0s/harbor-portal:${SHORT_SHA}"
